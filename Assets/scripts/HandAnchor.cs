// --- HandAnchor.cs (NEW SCRIPT) ---
using UnityEngine;

/// <summary>
/// Add this script to an empty GameObject that is a child of your hand model prefab.
/// Position and rotate this HandAnchor GameObject within the prefab to define the
/// exact point and orientation that should align with the PlayerShooter's stream spawn point.
/// </summary>
public class HandAnchor : MonoBehaviour
{
    // This script doesn't need any code. Its presence and Transform are what matter.
}